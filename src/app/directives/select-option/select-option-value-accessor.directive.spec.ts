import { SelectOptionValueAccessorDirective } from './select-option-value-accessor.directive';

describe('SelectOptionValueAccessorDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectOptionValueAccessorDirective();
    expect(directive).toBeTruthy();
  });
});
