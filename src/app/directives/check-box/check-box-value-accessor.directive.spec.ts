import { CheckBoxValueAccessorDirective } from './check-box-value-accessor.directive';

describe('CheckBoxValueAccessorDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckBoxValueAccessorDirective();
    expect(directive).toBeTruthy();
  });
});
