import { RadioCheckValueAccessorDirective } from './radio-check-value-accessor.directive';

describe('RadioCheckValueAccessorDirective', () => {
  it('should create an instance', () => {
    const directive = new RadioCheckValueAccessorDirective();
    expect(directive).toBeTruthy();
  });
});
