import { TextAreaValueAccessorDirective } from './text-area-value-accessor.directive';

describe('TextAreaValueAccessorDirective', () => {
  it('should create an instance', () => {
    const directive = new TextAreaValueAccessorDirective();
    expect(directive).toBeTruthy();
  });
});
