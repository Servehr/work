import { InputFieldValueAccessorDirective } from './input-field-value-accessor.directive';

describe('InputFieldValueAccessorDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFieldValueAccessorDirective();
    expect(directive).toBeTruthy();
  });
});
